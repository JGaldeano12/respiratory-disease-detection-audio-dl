## Models

The `models` directory is used to store trained models for inference.

* Custom convolutional neural networks will be saved in the `models/custom_cnn` directory.
* Pretrained models will be stored in the `models/pretrained` directory.

> **Note:** These directories are intentionally left empty due to the large size of model files.