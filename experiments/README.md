## Experiments

The `experiments` directory is used to store training metrics and experiment outputs.

* Training logs, evaluation metrics, and model performance results will be saved in the `experiments` directory.
* Each run may generate separate files to keep experiments organized and reproducible.

> **Note:** This folder may be empty to keep the directory structure clean.