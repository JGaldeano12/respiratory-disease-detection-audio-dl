## Data Directories

The `raw` and `processed` directories are intentionally left empty.

* Place the original (raw) data files in the `data/raw` directory.
* Running `generate_dataset.sh` will generate the processed dataset and store it in the `data/processed` directory.

> **Note:** These folders are excluded from version control to avoid uploading large datasets to the repository.